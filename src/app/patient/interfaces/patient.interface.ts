export interface Patient {
  //change UUID
  id: string
  firstName: string;
  lastName: string;
  ci: string;
  age: string;
  createdAt: Date;
  phoneNumber: string;
  amount: number;
  isActive: boolean;
  address: string;
}
